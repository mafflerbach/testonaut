<html>
  <head>
    <link href="{{ app.request.baseUrl }}/css/bootstrap.css" rel="stylesheet">
    <link href="{{ app.request.baseUrl }}/css/themes/{{app.theme}}.css" rel="stylesheet">
    <link rel="stylesheet/less" type="text/css" href="{{ app.request.baseUrl }}/css/style.less" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/less.js/2.5.0/less.min.js"></script>
    <script type="text/javascript">
      var baseUrl = '{{ app.request.baseUrl }}';
    </script>
    
    
    <style type="text/css">

      body { padding: 100px 15px }

      ol.breadcrumb {
        margin-bottom: 0;
      }
      .compare {
        clear: both;
        position: relative;

      }

      .compare .image {
        width: 300px;
        height: 300px;
        margin: 5px;
        float: left;
        overflow: hidden;
        border: 1px #ADADAD solid;
        box-shadow: 0px 1px 4px -2px #000000;
        position: relative;
        margin-top: 0;

      }
      .image img {
        max-width: 300px;
      }
      span.ops {
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        display: block;
        background-color: rgba(0, 0, 0, 0.5);
        height: 4em;
      }
      .compare h4 {
        padding: 1em 1.5em;
      }

      .compare span {
        position: absolute;
        color: rgba(255, 255, 255, 0.5);
        font-size: 1.5em;
      }

      .compare span.copy {
        right: 0.2em;
        top: 0.2em;
      }

      .compare span.delete {
        top: 1.5em;
        right: 0.2em;
      }

      span.desc {
        color: #fff;
        font-size: 0.7em;
        top: 0.8em;
        left: 0.5em
      }
      .screens h4 {
        clear:both;
      }
    </style>
    <script src="{{ app.request.baseUrl }}/js/vendor/jquery-2.1.3.js"></script>
    <script src="{{ app.request.baseUrl }}/js/vendor/advanced.js"></script>
    <script src="{{ app.request.baseUrl }}/js/vendor/wysihtml5-0.3.0.js"></script>
    <script src="{{ app.request.baseUrl }}/js/custom.js"></script>

  </head>
  <body>
