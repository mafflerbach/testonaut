{% include 'head.twig' %}
<div class="contentBox">
<div class="content">
  {% if app.request.message is defined %}
    <p>{{ app.request.message }}</p>

    <a href="{{ app.request.baseUrl }}/{{ app.request.path }}"
       class="backLink btn">Zurück</a>
  {% else %}

    <form action=""
          method="POST">
      Sind sie sicher das sie löschen möchten?
      <br/>
      <input type="submit"
             value="Ja"
             name="copy"
             class="btn btn-primary"/>
      <a href="{{ app.request.baseUrl }}/{{ app.request.path }}"
         class="backLink btn btn-link">Zurück</a>
    </form>

  {% endif %}
</div>
</div>

{% include 'footer.twig' %}
