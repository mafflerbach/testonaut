$(document).ready(function () {
/*
    $("a[data-action]").click(function (e) {
        e.preventDefault();
        var path = $(this).data('path');
        var action = $(this).data('action');

        $.ajax({
            url: 'index.php',
            type: 'POST',
            data: { path: path, action: action, content: content },
            dataType: "JSON"
        }).done(function (data) {
            console.log(data);
        });
    }); */
})

